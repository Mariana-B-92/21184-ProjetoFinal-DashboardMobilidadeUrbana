# Dependencias de EXECUCAO do dashboard (deploy no Hugging Face Spaces).
# Nao inclui ferramentas so necessarias na fase offline/local (py7zr, pytest):
# o modelo (mobilidade.db) ja vai construido no repositorio, via git-LFS.

# --- Processamento de dados ---
pandas>=2.2
numpy>=1.26

# --- Dados geoespaciais (operacoes em memoria no runtime: WKT, to_crs, buffer) ---
geopandas>=1.0
shapely>=2.0
pyproj>=3.6

# --- Dashboard ---
dash>=2.17
plotly>=5.22
dash-leaflet>=1.0
dash-extensions>=1.0      # usado em app/app.py (dash_extensions.javascript.assign)

# --- Servidor de producao ---
gunicorn>=21.2
